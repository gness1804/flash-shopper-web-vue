version: 2
jobs:
   build:
     docker:
       - image: circleci/node:4
     steps:
       - checkout
       - run:
           name: install
           command: npm install
      - run:
           name: lint
           command: npm run lint
